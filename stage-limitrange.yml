apiVersion: v1
kind: LimitRange
metadata:
  name: limit-1
  namespace: stage
spec:
  limits:
    - type: Container
      default:
        cpu: 200m
      max:
        cpu: 500m
      min:
        cpu: 50m
      maxLimitRequestRatio:
        cpu: "4"
